# Task: Real-time customSlots detection in bigLot for cars (no cooldown, log x/z/angle)\n\n## Plan Steps:\n1. **[x]** RoadAndGrass.js: Define/export `BIG_LOT_CUSTOM_SLOTS` array matching bigLot customSlots data. (Added export to ROAD_NETWORK)\n2. **[ ]** Car_clean.js: \n   - Add `this.customSlots` assignment in constructor (from roadNetwork).\n   - Add `findNearestCustomSlots(threshold)` method (return all nearby with dist).\n   - In `update()`: After coord points block, find/log all nearby slots' x/z/angle.\n3. **[ ]** Test: Run scene, approach bigLot, verify console logs every frame.\n\n## Progress:\nStep 1 complete. Next: Car_clean.js implementation.

